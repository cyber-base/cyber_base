{% extends 'base.html.twig' %}

{% block title %}Animateur
{% endblock %}

{% block body %}
<h3 class="text-center shadow p-3 bg-secondary bg-opacity-10 w-50 mx-auto">Usagers par ateliers</h3>

<div class="p-3 mb-2 bg-body border rounded mx-auto">
	<table class="table p-3 mb-2 bg-body border rounded mx-auto w-50">

		<tbody>
			<tr>
				<th>ID</th>
				<td>{{ UsagersByAteliers.usagers.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{UsagersByAteliers.usagers.nom }}</td>
			</tr>
			<tr>
				<th>Prénom</th>
				<td>{{ UsagersByAteliers.usagers.prenom }}</td>
			</tr>
			<tr>
				<th>Téléphone</th>
				<td>{{ UsagersByAteliers.usagers.tel }}</td>
			</tr>
			<tr>
				<th>E-mail</th>
				<td>{{UsagersByAteliers.usagers.email }}</td>
			</tr>
			<tr>
				<th>Poste</th>
				<td>{{{{UsagersByAteliers.poste }} }}</td>
			</tr>
			<tr>
			<th>Adresse</th>
			<td>{{{{UsagersByAteliers.usagers.adresse }} }}</td>
		</tr>
	<th>Ville</th>
	<td>{{{{UsagersByAteliers.usagers.ville }} }}</td>
</tr>
			
		</tbody>

	</table>


	<div class="d-grid gap-2 d-md-flex justify-content-md-center">
	<a href="{{ path('app_animateur_index') }}" class="btn btn-secondary">Retour</a>

	{# <a href="{{ path('app_animateur_edit', {'id': animateur.id}) }}" class="btn btn-primary">Modifier</a> #}

	{# {{ include('animateur/_delete_form.html.twig') }} #}
	</div>
	</div>
    
{% endblock %}
