<form method="post" action="{{ path('app_quartier_delete', {'id': quartier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quartier.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
