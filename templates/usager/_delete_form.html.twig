<form method="post" action="{{ path('app_usager_delete', {'id': usager.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usager.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
